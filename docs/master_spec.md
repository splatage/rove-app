# Deprecated filename: use `master-spec.md`

This file exists only as a compatibility alias.

The canonical master specification is:

- `docs/master-spec.md`

Reason:
- the repository previously contained both `master-spec.md` and `master_spec.md`
- keeping two full copies risks documentation drift
- this file now intentionally redirects readers to the canonical document
